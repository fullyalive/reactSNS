// function sum(a, b) {
//   return a + b;
// }

// const sum = (a, b) => {
//   return a + b;
// };

const sum = (a, b) => a + b; // only 1 line

const plusOne = a => a + 1; // one parameter;

module.exports = {
  sum,
  plusOne
};
